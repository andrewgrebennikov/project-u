import { Counter } from './ui/Counter';
import type { CounterState } from './model/types/counterState';
import { counterReducer } from './model/slices/counterSlice';

export { Counter, CounterState, counterReducer };
